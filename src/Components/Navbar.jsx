export default function Navbar({title}){
    return(
        <>z
            <div className="">
                <button className="font-semibold text-stone-500 hover:text-orange-600">{title}</button>
            </div>
        </>
    )
}